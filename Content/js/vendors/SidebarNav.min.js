
(function (a) {
    "use strict";
    a.fn.SidebarNav = function (b) {
        var c = a.extend({
            animationSpeed: 300
        }, b);
        return this.each(function () {
            a(this).on("click", "li a", function (b) {
                var d = a(this).next();
                if (d.is(".treeview-menu") && d.is(":visible")) d.slideUp(c.animationSpeed, function () {
                    d.removeClass("menu-open")
                }), d.parent("li").removeClass("active");
                else if (d.is(".treeview-menu") && !d.is(":visible")) {
                    var e = a(this).parents("ul").first(),
                        f = e.find("ul:visible").slideUp(c.animationSpeed);
                    f.removeClass("menu-open");
                    var g = a(this).parent("li");
                    d.slideDown(c.animationSpeed, function () {
                        d.addClass("menu-open"), e.find("li.active").removeClass("active"), g.addClass("active")
                    })
                } else {
                    var g = a(this).parent("li");
                    a(".treeview-menu li").removeClass("active");
                    $(g).addClass("active");
                }
                d.is(".treeview-menu") && b.preventDefault()
            })
        })
    }
})(jQuery);
